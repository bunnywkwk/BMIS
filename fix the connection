## Setup Instructions

1. Clone the repository:
2. Open `BMIS.sln` in Visual Studio.

3. Restore the database:
- Open SQL Server Management Studio (SSMS).
- Run the script from `/Database/bmis_database.sql`.

4. Update `config.txt` with your SQL Server connection details.

5. Build and run the project.
